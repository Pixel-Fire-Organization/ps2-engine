# This directory contains raw assets for authoring.
# Each asset is a pair: <name>.json (metadata) + <name>.<ext> (source file).
# Run `scripts/pack_assets.py` to compile them into .ps2a files in rassets/.


# CMake Build System
build/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile

# Engine resources
assets/maps/autosave/**

# Executables Outputs and Release Folders
dist/
bin/

# Object Files and Static Libraries
*.o
*.obj
*.a
*.lib

# PlayStation 2 Specific Artifacts
*.elf
*.iso
SYSTEM.CNF

# Environments & IDEs
.clangd
.vscode/
.vs/
.idea/
*.swp
*~

# Operating System Files
.DS_Store
Thumbs.db

# Engine assets (On ISO Generation)
*.ps2a
build_log.txt

# ...but keep test golden fixtures and input fixtures even if they match the patterns above.
!tools/tests/golden/**
!tools/tests/fixtures/**

# PCSX2 artefacts
.pcsx2/**

# Vita shader compiler.
#
# psp2cgc is Sony's Cg compiler, redistributed by third parties rather than
# licensed for redistribution. The Vita graphics backends need it to compile
# shaders at build time, but this project must not carry another copy of it:
# each developer fetches their own, and the build requires it explicitly rather
# than falling back to something worse. See docs/vita/BUILD.md.
external/psp2cgc/

# Python bytecode cache
__pycache__/
.pytest_cache/